# Build artifacts
/target/
server/target/
**/*.rs.bk
*.pdb

# Cargo.lock - ignore for both extension and server
Cargo.lock
server/Cargo.lock

# IDE files
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# Extension build artifacts
*.wasm
extension.zip

# Package directory (no longer used)
auto-header-package/
*.tar.gz

# Old scripts (keeping for reference but not needed)
install.sh
package.sh
